recvfrom()会返回已写入由buffer参数指定的缓冲区的消息长度，
对于机遇消息的套接字，例如SOCK_RAW,SOCK_DGRAM和SOCK_SEQPACKET，
整个消息会在单个操作中被读入。如果消息太长超出了提供的缓冲区，
并且没有在flag参数中设置MSG_PEEK，超出的字节将被丢弃。
对于基于流的套接字，例如SOCK_STREAM，消息界限将被忽略。在这种情况下，
数据一旦可用就会立即返回给用户，并且没有数据会被丢弃。
